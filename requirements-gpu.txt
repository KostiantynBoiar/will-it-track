# SAM-3 zero-shot transfer study — GPU inference box.
# Adds the frozen SAM 3 backbone + the OFFICIAL VEval scorer on top of the local analysis stack.
# A single modern GPU (24-40 GB) suffices — inference only.
#
# SAM 3 and VEval are NOT PyPI packages — vendor them under third_party/ (gitignored) as a pinned
# git clone with a VENDORED_COMMIT.txt, then install editable, e.g.:
#   git clone https://github.com/facebookresearch/sam3 third_party/sam3   # SAM 3 + the VEval scorer
#   .venv/bin/pip install -e third_party/sam3

-r requirements-local.txt

# --- pinned via third_party/ (documented, not pip-resolved) ---
# sam3  @ third_party/sam3   # frozen promptable backbone + official VEval scorer
